'use client';import {MapPinned,Navigation,Plus,Minus,LocateFixed} from 'lucide-react';import {Centre} from '@/types';
export default function MapMock({centres,onSelect,selected}:{centres:Centre[];onSelect?:(c:Centre)=>void;selected?:string}){return <div className="map-grid relative min-h-[430px] overflow-hidden rounded-2xl border border-[#dce8e0]"><div className="absolute inset-0 opacity-50" style={{backgroundImage:'radial-gradient(circle at 25% 25%,rgba(13,92,58,.10) 0 2px,transparent 3px),radial-gradient(circle at 75% 65%,rgba(13,92,58,.10) 0 2px,transparent 3px)',backgroundSize:'70px 70px'}}/><div className="absolute left-[47%] top-[48%] flex -translate-x-1/2 -translate-y-1/2 flex-col items-center"><div className="flex h-11 w-11 items-center justify-center rounded-full border-4 border-white bg-forest text-white shadow-lg pulse-dot"><LocateFixed size={18}/></div><div className="mt-1 rounded-full bg-white px-2 py-1 text-[10px] font-bold shadow">You · Ahmedabad</div></div>{centres.map((c,i)=>{const positions=[['21%','32%'],['70%','24%'],['63%','73%'],['24%','76%'],['79%','54%']][i%5];return <button key={c.id} onClick={()=>onSelect?.(c)} className={`absolute -translate-x-1/2 -translate-y-1/2 transition ${selected===c.id?'scale-110':''}`} style={{left:positions[0],top:positions[1]}}><div className={`flex h-9 w-9 items-center justify-center rounded-full border-4 border-white shadow-lg ${selected===c.id?'bg-forest':'bg-leaf'}`}><MapPinned size={15} className="text-white"/></div><div className="mt-1 whitespace-nowrap rounded-lg bg-white px-2 py-1 text-[10px] font-bold shadow">{c.distance} km · {c.waiting}m</div></button>})}<div className="absolute left-4 top-4 rounded-xl bg-white/90 p-2 shadow"><button className="block p-1.5 text-[#65736b]"><Plus size={17}/></button><div className="h-px bg-[#e3ece6]"/><button className="block p-1.5 text-[#65736b]"><Minus size={17}/></button></div><div className="absolute bottom-4 left-4 right-4 flex items-center justify-between rounded-xl border border-white/70 bg-white/90 px-3 py-2 text-xs font-semibold text-[#617068] shadow backdrop-blur"><span>Demo map · centre availability updates from mock service</span><span className="hidden items-center gap-1 text-forest sm:flex"><Navigation size={14}/> Directions supported</span></div></div>}
