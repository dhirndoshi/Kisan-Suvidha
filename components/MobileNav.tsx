'use client';
import Link from 'next/link';import {Home,Leaf,CalendarClock,PackageCheck,UserRound} from 'lucide-react';import {usePathname} from 'next/navigation';
export default function MobileNav(){const path=usePathname();const items=[['Home','/dashboard',Home],['Crops','/crops',Leaf],['Book','/book',CalendarClock],['Tracking','/tracking',PackageCheck],['Profile','/profile',UserRound]] as const;return <nav className="fixed inset-x-0 bottom-0 z-40 border-t border-[#dfe9e2] bg-white/95 px-2 pb-[env(safe-area-inset-bottom)] pt-2 shadow-[0_-10px_30px_rgba(16,35,26,.07)] backdrop-blur lg:hidden"><div className="mx-auto flex max-w-md items-center justify-around">{items.map(([label,href,Icon])=><Link key={href} href={href} className={`flex min-w-[62px] flex-col items-center gap-1 rounded-xl px-2 py-1.5 text-[10px] font-bold ${path===href?'text-forest':'text-[#7d8982]'}`}><Icon size={19}/><span>{label}</span></Link>)}</div></nav>}
